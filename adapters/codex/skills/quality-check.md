# quality-check

リポジトリルートからの相対パスで `methodology/skills/quality-check.md` を読み、そこに定義された手順（プロジェクト設定確認 → auto-fix → lint → 型チェック → テストの順序 → 結果フォーマット → 失敗時対応）に従って実行すること。

手順・判定基準・出力フォーマット（人間向けサマリー / 機械可読JSON）の実体はすべて参照先ドキュメントに定義されている。ここでの重複記述は行わない。
