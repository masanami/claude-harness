# {PROJECT_NAME} - Claude Code プロジェクトコンテキスト

## 開発規約

### ブランチ・コミット
- **方針**: 1チケット = 1ブランチ → PR → 必須ゲート通過後にマージ（{BRANCH_STRATEGY}）
- **ブランチ**: `{BRANCH_FORMAT}`
- **コミット**: Conventional Commits + {COMMIT_LANGUAGE}
  - type: `feat`, `fix`, `docs`, `style`, `refactor`, `perf`, `test`, `chore`, `ci`
  - scope: {SCOPES}
- **PR**: ≤{MAX_PR_LINES}行、{MERGE_STRATEGY}

### 新規ファイルの置き場
{NEW_FILE_PLACEMENT}

## テスト方針

- {TEST_APPROACH}
- Mock対象: {MOCK_TARGETS}
- Mockしない: {NO_MOCK_TARGETS}

## 品質方針

```
{QUALITY_POLICY}
```

## よく使うコマンド

```bash
{COMMON_COMMANDS}
```

> このファイルは 200 行未満に保つ。肥大したらセッション内 `/doctor`（Claude Code v2.1.206 以降）の trim 提案に従い、コードベースから導ける内容（ディレクトリ構成・依存一覧・アーキテクチャ概要）を削る。
