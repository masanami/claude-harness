# {機能名}

## 概要

{1-2文で機能の概要}

## 背景・目的

{なぜこの機能が必要か}

## ユーザーストーリー

{ペルソナ}として、{機能}を使って、{目的}を達成したい。

## 機能要件

- [ ] {要件1}
- [ ] {要件2}

## 非機能要件（該当する場合）

- パフォーマンス: {要件}
- セキュリティ: {要件}

## 技術的な制約・方針

- 使用技術: {技術スタック}
- 変更対象: {ディレクトリやファイル}
- 既存コードとの関係: {依存する既存機能}

## 画面・API設計（該当する場合）

{画面モックやAPIスキーマ}

## クリティカル設計決定（該当する場合）

> セキュリティ・機密データ・外部連携・DBスキーマ等のクリティカル領域について、要件定義時に決定した方針を記録する。後続の `feature-implementer` はこの決定に従い、独自判断で逸脱しない。

### {領域名 例: 認可モデル}

- **採用案**: {決定した案}
- **理由**: {採用理由}
- **代替案**: {検討した他案とその却下理由}
- **影響範囲**: {変更対象モジュール・既存資産との関係}

該当しない領域は **セクションごと削除する**。クリティカル領域が無ければこのセクション自体を削除してよい。

## 機能全体の設計（複雑な機能のみ）

> 複数チケットにまたがるアーキ判断・IF/API・データモデルを記録する。シンプルな機能では削除してよい。

### アーキテクチャ決定

{実装方針と理由、変更対象モジュール、既存コードとの関係}

### IF / API（該当する場合）

{モジュール間の境界となるインターフェース契約}

### データモデル（非クリティカル分・該当する場合）

{データ構造の定義。クリティカルなスキーマ変更は「クリティカル設計決定」セクションへ}

### 実装計画（チケット分解の見通し）

{実装対象スライス内のチケット分解の方針案。最終分解は /create-ticket で行う}

## スライス（出荷の単位）

> 必須節。削除しない。スライスは「それだけで出荷でき、価値が出る縦切り」であり、チケット分解の粒度（`/create-ticket` 実装分解モード）とは別の軸。S1 はそれだけで価値が出る最小の縦切り。S2 以降は後続スコープであり、前スライスの PR がマージされてから起票する。スライスが1つだけでも S1 の行は残す。`実装対象:` には表に在るスライス ID を1つ書く（初期作成時は S1。後続スライスへ進めるときに書き換える）。「触るファイル数（概算）」は半角数字の単一値か `下限-上限` の範囲で書く。

| スライス | 内容 | 触るファイル数（概算） | 出荷条件 |
|---|---|---|---|
| S1（最小） | {それだけで価値が出る最小の縦切り} | {3-5} | これだけで価値が出る |
| S2 | {後続スコープ} | {N} | S1 がマージされてから |

実装対象: S1

## やらないこと

> 必須節。削除しない。この機能の範囲外を明示列挙する（1件以上）。書いていないことを範囲外とみなさない。後続スライス（S2 以降）はここではなくスライス表に書く。

- {範囲外1}（理由: {やらない理由}）
- {範囲外2}（理由: {やらない理由}）

## 受入基準

> 受入基準は「実装対象」のスライス（既定 S1）の範囲に収める。後続スライスの基準はここに書かない。

- [ ] {基準1}
- [ ] {基準2}
