# {タスク名}

Parent: #{親チケット番号}

## 概要

{1-2文でタスクの概要を説明}

## 関連ドキュメント

- 要件ドキュメント: `docs/requirements/{slug}.md`
- 設計ドキュメント: `docs/design/{slug}.md`（該当する場合）

> 親チケット #{親チケット番号} の **「クリティカル設計決定」** セクションを必読のこと（認可モデル・暗号化方式・スキーマ方針・外部API契約など）。実装時に独自判断で逸脱しない。

## 背景

{タスクの背景・コンテキスト}

## 完了条件

- [ ] {条件1}
- [ ] {条件2}
- [ ] 単体テストが追加されている
- [ ] 既存テストがパスする

## 技術的な指示

### 変更対象ファイル

- `{ファイルパス}`（新規作成 / 変更）

### 参照すべきコード・パターン

- `{参照コードパス}` - {パターン説明}

### 制約

- {制約事項}

## 関連情報

### APIスキーマ（該当する場合）

```typescript
// リクエスト
type Request = {
  // ...
};

// レスポンス
type Response = {
  // ...
};
```

## 依存チケット

- #{依存チケット番号}: {説明}（{ステータス}）
