<!-- ADR テンプレート。`<ADR置き場>/{NNNN}-{slug}.md` として保存する。
     `<ADR置き場>` は実行時に解決した ADR の保存先（既定 `docs/adr/`。導入先の CLAUDE.md の
     ドキュメントマップで別パスが指定されていればそれ）。保存先を固定で `docs/adr/` にしない。
     該当しない行・セクションは、空欄や「特になし」で埋めずに削除すること。
     このコメント自体も保存時に削除する。 -->

# ADR {NNNN}: {決定の要約（見出しだけで結論が読み取れる短文）}

- **ステータス**: Accepted
- **日付**: {YYYY-MM-DD}
- **関連**: {Issue/PR 番号、退役元の機能仕様パス、関連 ADR。無ければ行ごと削除}

## 背景

{何が問題で、なぜ決める必要があったか。決定時点の前提・制約（技術・組織・法令・既存実装）を書く。
 前提が変われば決定を見直せるよう、「決定時点で真だったこと」を明示する。}

## 決定

{採用した案を**断定形**で書く（「〜とする」）。今後の実装が従うべき規範として読める粒度にする。
 適用範囲（どこまでに効くか）と、明示的にスコープ外とするものも書く。}

## 検討した代替案

| 案 | 内容 | 却下した理由 |
|---|---|---|
| {案A} | {概要} | {なぜ採らなかったか。ここが本体——理由が無いと同じ提案が再燃する} |
| {案B} | {概要} | {同上} |

## 帰結

- **利点**: {この決定によって得られるもの}
- **コスト・制約**: {受け入れた不利益・今後負い続ける制約}
- **将来の見直し条件**: {どうなったら再検討すべきか。無ければ行ごと削除}

<!-- 未決の論点が残る場合のみ、以下の節を残す。決めたふりをしない。
## 未決事項

- {論点}: {何が分かれば決められるか}
-->

<!-- 決定を改訂するときのみ、以下の節を末尾に追記する（本文は書き換えない）。
     あわせて冒頭のステータスを Amended にし、日付行に改訂日を追記する。
## 改訂（YYYY-MM-DD）

### 経緯

{何が変わって改訂に至ったか}

### 改訂後の決定

{新しい決定。元の「決定」節のどの部分を置き換えるかを明示する}
-->
